<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="studymember">

	<select id="studyMemberOne" resultType="studymember"
		parameterType="string">
		select * from study_member where email=#{value}
	</select> 

	<insert id="insertStudyMember" parameterType="studymember">
		insert into study_member (email, password, nickname, name, tel, picture, point, joindate)
		values(#{email},#{password},#{nickname},#{name},#{tel},#{picture}, 50, sysdate)
	</insert>

    <select id="studyMemberIdExist" parameterType="string" resultType="int">
        select count(*) from study_member where email = #{value}
    </select> 
    
    <select id="studyMemberNicknameExist" parameterType="string" resultType="int">
        select count(*) from study_member where nickname = #{nickname}
    </select> 
    
    

</mapper>